services:
  - type: web
    name: freefire-info-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: TOKEN
        sync: false
      - key: PORT
        value: 10000
